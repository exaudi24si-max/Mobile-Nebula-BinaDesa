<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Exaudinebula" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary_desa</item>
        <item name="colorPrimaryVariant">@color/primary_dark_desa</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Status Bar Color -->
        <item name="android:statusBarColor">@color/primary_desa</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="Theme.Exaudinebula" parent="Base.Theme.Exaudinebula" />
</resources>